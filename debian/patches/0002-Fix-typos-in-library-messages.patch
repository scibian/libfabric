From: Mehdi Dogguy <mehdi@debian.org>
Date: Tue, 19 Jun 2018 07:58:59 +0000
Subject: Fix typos in library messages

---
 prov/psm2/src/psmx2_av.c | 2 +-
 prov/rxm/src/rxm_ep.c    | 2 +-
 prov/rxm/src/rxm_rma.c   | 4 ++--
 3 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/prov/psm2/src/psmx2_av.c b/prov/psm2/src/psmx2_av.c
index 4999bba..382c72f 100644
--- a/prov/psm2/src/psmx2_av.c
+++ b/prov/psm2/src/psmx2_av.c
@@ -236,7 +236,7 @@ int psmx2_epid_to_epaddr(struct psmx2_trx_ctxt *trx_ctxt,
 			      epaddr, psmx2_conn_timeout(1));
 	if (err != PSM2_OK) {
 		FI_WARN(&psmx2_prov, FI_LOG_AV,
-			"psm2_ep_connect retured error %s, remote epid=%lx.\n",
+			"psm2_ep_connect returned error %s, remote epid=%lx.\n",
 			psm2_error_get_string(err), epid);
 		return psmx2_errno(err);
 	}
diff --git a/prov/rxm/src/rxm_ep.c b/prov/rxm/src/rxm_ep.c
index 683cb6c..ac0a9c7 100644
--- a/prov/rxm/src/rxm_ep.c
+++ b/prov/rxm/src/rxm_ep.c
@@ -941,7 +941,7 @@ void rxm_ep_handle_postponed_tx_op(struct rxm_ep *rxm_ep,
 
 	tx_entry->tx_buf->pkt.ctrl_hdr.conn_id = rxm_conn->handle.remote_key;
 	FI_DBG(&rxm_prov, FI_LOG_EP_DATA,
-	       "Send deffered TX request (len - %"PRIu64") for %p conn\n",
+	       "Send defered TX request (len - %"PRIu64") for %p conn\n",
 	       tx_entry->tx_buf->pkt.hdr.size, rxm_conn);
 
 	if ((tx_size <= rxm_ep->msg_info->tx_attr->inject_size) &&
diff --git a/prov/rxm/src/rxm_rma.c b/prov/rxm/src/rxm_rma.c
index 4e7e56f..514ac61 100644
--- a/prov/rxm/src/rxm_rma.c
+++ b/prov/rxm/src/rxm_rma.c
@@ -189,7 +189,7 @@ void rxm_ep_handle_postponed_rma_op(struct rxm_ep *rxm_ep,
 	struct fi_cq_err_entry err_entry;
 
 	FI_DBG(&rxm_prov, FI_LOG_EP_DATA,
-	       "Perform deffered RMA operation (len - %"PRIu64") for %p conn\n",
+	       "Perform defered RMA operation (len - %"PRIu64") for %p conn\n",
 	       tx_entry->rma_buf->pkt.hdr.size, rxm_conn);
 
 	if (tx_entry->comp_flags & FI_WRITE) {
@@ -220,7 +220,7 @@ void rxm_ep_handle_postponed_rma_op(struct rxm_ep *rxm_ep,
 	return;
 err:
 	FI_WARN(&rxm_prov, FI_LOG_EP_DATA,
-		"Unable to perform deffered RMA operation\n");
+		"Unable to perform deferred RMA operation\n");
 
 	memset(&err_entry, 0, sizeof(err_entry));
 	err_entry.op_context = tx_entry->context;
